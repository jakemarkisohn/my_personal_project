// import { useState } from "react";
// import { recipe } from "./Recipes"

// export const Recipe = ({ recipe }) => {
//     const [recipes, setRecipes] = useState(recipe.recipe_book);
//     return (
//         <div id={`recipe book${recipe_book.id}`}>
//             <h3>{recipe_book.title}</h3>
//                 {recipes.map((recipe) => (
//                   <Recipe key={`recipe${recipe.id}`} recipe={recipe}/>
//                 ))}
//         </div>
//     )

// }